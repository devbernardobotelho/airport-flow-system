const StandStatus = {
  FREE: 'FREE',
  OCCUPIED: 'OCCUPIED'
};

const StandType = {
  GATE: 'GATE',
  REMOTE: 'REMOTE'
};

const Priority = {
  NORMAL: 'NORMAL',
  EMERGENCY: 'EMERGENCY'
};

const FlightStatus = {
  WAITING: 'WAITING',
  APPROACHING: 'APPROACHING',
  LANDED: 'LANDED'
};

module.exports = {
  StandStatus,
  StandType,
  Priority,
  FlightStatus
};
